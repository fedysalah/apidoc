openapi: 3.0.0
info:
  title: Api Test
  version: '1.0'
  description: ''
servers: []
paths:
  /users:
    get:
      summary: List users
      tags:
        - Users
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/User'
        '404':
          description: Not Found
      operationId: get-users
components:
  schemas:
    User:
      title: User
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        lastName:
          type: string
        '':
          type: string
  securitySchemes:
    Token:
      type: http
      scheme: bearer
tags:
  - name: Users
